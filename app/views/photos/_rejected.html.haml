%h2 There is a problem with this photo
= image_tag(@photo.image.url, :style => "float: left; margin-right: 30px;")
%p
  This photo is not being matched because
  =@photo.rejected_reason || "it is not acceptable"

= link_to(image_tag("changephoto.png"), edit_photo_path(@photo))

